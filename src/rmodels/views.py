from django.shortcuts import render

def createmodel(request):
    pass
    # if request.method == 'GET':
    #     return render(request, "{% url 'createmodel' %}", {'form': RmodelForm()})
    # else:
    #     form = RmodelForm(request.POST)
    #     newmodel = form.save(commit=False)
    #     newmodel.user = request.user
    #     newmodel.save()


